# CMU 10-799: Diffusion & Flow Matching
# Base requirements (install PyTorch separately based on your platform)
#
# Usage:
#   1. First install PyTorch for your platform (see requirements-*.txt files)
#   2. Then: pip install -r environments/requirements.txt


# Dataset loading
datasets>=2.0.0
einops>=0.6.0
huggingface-hub>=0.16.0
ipykernel>=6.0.0

# Jupyter notebooks
jupyter>=1.0.0
matplotlib>=3.4.0
# Core dependencies
numpy>=1.21.0  # Pin to 1.x for PyTorch compatibility

# Evaluation
pandas>=1.3.0
pillow>=9.0.0
PyYAML>=6.0
scipy>=1.9.0
torch-fidelity>=0.3.0  # Comprehensive metrics: FID, KID, IS
tqdm>=4.64.0

# Experiment tracking (optional but recommended)
wandb>=0.13.0
