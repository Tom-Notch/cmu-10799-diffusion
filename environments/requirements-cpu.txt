# CMU 10-799: Diffusion & Flow Matching
# CPU-only PyTorch installation
#
# Use this for:
#   - Local development on laptop (no GPU)
#   - Testing code before running on Modal/cloud
#   - macOS users
#
# Installation:
#   pip install -r environments/requirements-cpu.txt

--extra-index-url https://pypi.org/simple
--index-url https://download.pytorch.org/whl/cpu

# Include base requirements
-r requirements.txt

# Modal CLI for cloud training
modal>=0.63.0

torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0
